/** Direct browser capabilities. This entry intentionally has no React or MUI dependency. */
export {
  currentPathname,
  currentUrl,
  isLocalhost,
  localFlag,
  navigateTo,
  onAuthChange,
  onBeforeUnload,
  onGlobalKeydown,
  onWindowResize,
  queryParam,
  reloadPage,
  replaceUrl,
  viewportSnapshot,
} from '../browser';
