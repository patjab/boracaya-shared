/** Direct browser capabilities. This entry intentionally has no React or MUI dependency. */
export {
  currentPathname,
  currentUrl,
  isLocalhost,
  localFlag,
  navigateTo,
  onAuthChange,
  onBeforeUnload,
  onDocumentClick,
  onGlobalKeydown,
  onUncaughtErrors,
  pageTelemetryContext,
  onWindowResize,
  queryParam,
  reloadPage,
  replaceUrl,
  viewportSnapshot,
} from '../browser';
